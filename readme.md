# Venv 

```bash
source venv/bin/activate
```

# freeze

pip freeze > requirements.txt
pip install -r requirements.txt

# libs

- streamlit (basico)
- sqlachemy (basico) (join, busca complexa)
- pandas (sum, avg, count)
- numpy (sum, avg, count)


# não esquecer

```python
df = pd.DataFrame(t.__dict__ for t in tasks)
```
 